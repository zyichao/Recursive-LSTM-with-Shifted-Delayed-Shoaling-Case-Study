# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# ============================================================
# Virtual environments
# ============================================================
venv/
cir_env/
.venv/

# ============================================================
# OS cruft
# ============================================================
.DS_Store



# Upstream QC stage output (preprocess_sedimentation.py) -- large diagnostic
# CSVs + figures, regenerated by re-running the script.
data/processed/sed_preprocessing/

# Upstream smoothing stage's large intermediate interval table + diagnostic
# figures (student_t_kalman_smoother.py); reach_daily_studentt.csv (the
# final daily table actually used downstream) is kept.
data/processed/student_t_smoothing/reach_interval_CIR_studentt.csv
data/processed/student_t_smoothing/figures/

# ML-stage windowed tensors/scalers/sample metadata (run_main.py)
data/processed/ml_input/

# Trained model checkpoints (run_main.py)
saved_models/*.pt
saved_models/*.pth

# Run outputs: predictions, metrics, figures (run_main.py / evaluation/results.py)
results/
